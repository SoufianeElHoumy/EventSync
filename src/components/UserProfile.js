import React from 'react';

const UserProfile = () => (
  <div className='container'>
    <h1>User Profile</h1>
    <p>Manage your saved and attended events here.</p>
  </div>
);

export default UserProfile;
